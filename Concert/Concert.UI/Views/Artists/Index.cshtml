@model IEnumerable<ArtistDto>

<h1 class="mt-3">Artists</h1>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var artist in Model)
        {
            <tr>
                <td>@artist.Id</td>
                <td>@artist.Name</td>
            </tr>
        }
    </tbody>
</table>